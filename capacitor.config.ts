import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.devraj.gymtracker',
  appName: 'Gym Workout Tracker',
  webDir: 'dist'
};

export default config;
